source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'
project 'Sorter'

target 'Sorter' do
    pod 'SwiftLint', '0.18.1', :configurations => 'Debug'
end

target 'SorterTests' do
    use_frameworks!
    inherit! :search_paths

    pod 'Quick', '1.1.0'
    pod 'Nimble', '6.0.1'
end
